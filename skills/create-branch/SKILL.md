---
name: create-branch
description: "为任务创建新的 Git 分支。当用户需要为新任务创建分支、请求推荐分支名称、说'创建分支'、'开个分支'、'切个分支'、'推荐分支名'、'分支叫什么'、'命名分支'、'起个分支名'、'分支建议'时调用。"
---

# 创建 Git 分支

为当前任务推荐分支名称并从当前分支切出子分支。**只创建本地分支并切换，不推送、不提交。**

## 工作流

1. **查看项目现有分支命名习惯**
   ```bash
   git branch -a
   ```
   分析已有分支（含远端 `remotes/origin/*`）的前缀类型与命名格式，保持约定一致。

2. **提取任务意图**：从用户请求与对话上下文判断任务的**类型**与**描述**

   - 类型：新功能→`feat`、修复→`fix`、优化→`optimize`、重构→`refactor`
   - 描述：从用户原话中提取 2~4 个英文关键词，kebab-case 连接
   - 示例："帮我开个分支，我要做托盘菜单优化" → `optimize/tray-menu`
   - **若任务类型或内容不明确（如用户只说"推荐分支名称"且无上下文），先向用户询问任务类型与内容，得到答复后再推荐，不要凭空猜测**

3. **按任务类型推荐分支名**（格式：`类型/英文描述`）

   本项目分支命名约定为**小写 kebab-case**（连字符分隔的英文小写描述），常见前缀：

   | 任务类型 | 前缀 | 示例 |
   |---------|------|------|
   | 新功能 | `feat/` | `feat/color-distribution-analysis` |
   | 修复 | `fix/` | `fix/windows-taskbar-icon-blur` |
   | 优化 | `optimize/` | `optimize/tray-menu` |
   | 重构 | `refactor/` | `refactor/code-cleanup` |

   - 描述需**精准反映任务内容**（如"任务栏图标模糊"→ `taskbar-icon-blur`），避免过于笼统
   - 若任务横跨多个方面，选最主要的意图定前缀
   - 在响应中向用户说明推荐理由（与现有约定的对应关系），并给出一个明确的推荐名称

4. **检查分支名是否可用**（防止与已有本地/远端分支重名）
   ```bash
   git branch -a | grep <分支名>
   ```
   **必须用 `-a` 查全量**：`git branch --list` 只查本地分支，会漏掉远端已有的同名分支，创建后推送到远端才会冲突。若输出非空（含 `remotes/origin/`），建议加序号后缀（如 `-2`）或换描述词后重新推荐。

5. **从当前分支切出子分支**
   ```bash
   git checkout -b <分支名>
   ```

6. **验证切换成功**
   ```bash
   git branch --show-current
   ```
   确认输出为刚创建的分支名。

7. **告知用户**：工作区未提交的改动会随分支切换保留，分支尚未推送，提交前可随时改名或删除。

## 注意

- 只在用户明确表达创建分支意图时执行（"创建分支"、"开个分支"等）；若用户只问命名规则而未要求创建，仅推荐名称，不执行 `checkout -b`
- 不执行 `git push`，不自动提交；用户要求提交时另行处理
- 若用户对推荐名称不满意，用 `git branch -m <新名>` 重命名即可，无需删除重建
- 若当前分支已是某条非主线分支（如已在一个 `fix/` 分支上）且与本次任务无关，可提醒用户：基于主线（`main`/`master`）切出会更干净；但用户明确要求"从当前分支切出"时，遵从用户意图
