# 云构建：在 GitHub Actions 上打包 Windows 安装包，无需本地安装 Rust / Node 环境。
# 触发方式：仅 Actions 页面手动运行（workflow_dispatch），推送标签等场景不触发。
# 产物：上传为 Artifact；若手动运行时选择的 ref 是标签（refs/tags/*），同时发布到该 Release。
# 详细使用步骤见 docs/使用说明.md 的「云构建」章
name: 构建安装包

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write # 发布 Release 需要
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: |
            package-lock.json
            installer/package-lock.json

      - name: 安装 Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust 编译缓存（主应用 + 安装器）
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            src-tauri -> target
            installer/src-tauri -> target

      # npm ci 要求 lockfile 与 package.json 严格一致（快且干净）；
      # 若 fork 后改了依赖却忘同步 lockfile，回退到 npm install 以免云构建直接失败
      - name: 安装依赖
        run: |
          npm ci || npm install
          npm --prefix installer ci || npm --prefix installer install

      # 首次构建无缓存需完整编译两个 Rust 项目，约 15~25 分钟；后续有缓存会快很多
      - name: 构建安装包
        run: npm run build

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: installer
          path: dist-installer/*.exe

      # 手动运行时在 ref 下拉框选择标签（如 v0.1.0）即命中此步
      - name: 发布到 Release（ref 为标签时）
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist-installer/*.exe
