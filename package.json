{
  "name": "my-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:installer": "npm --prefix installer run tauri dev",
    "test:tray": "node scripts/test-tray-opt-in.mjs",
    "build": "node scripts/build-installer.mjs",
    "build:nsis": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "html-minifier-terser": "^7.2.0",
    "serve": "^14.2.6"
  }
}
