{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-menu",
  "description": "Capability for the tray menu window (minimal permissions)",
  "windows": ["tray-menu"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-size"
  ]
}
