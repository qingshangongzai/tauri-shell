# 轻壳（Tauri Shell）开发规范

> 本文件是本项目所有编码 / 审查 / 提交规则的**唯一真源**。任何 skill、文档、对话约定与本文件冲突时，以本文件为准。

## 1. 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 壳 | Tauri 2（Rust） | `src-tauri/`，仅窗口适配、托盘、退出等轻逻辑 |
| 前端 | **原生 HTML / CSS / JS，单文件** | `dist/index.html`，零依赖、资源全内联，既是源码也是产物 |
| 托盘菜单 | 原生单文件 HTML | `dist/tray-menu.html`，自绘菜单页 |
| 安装器 | Tauri 2 + React 19 + TypeScript + Vite + Tailwind CSS 4 | `installer/` 独立子包，自带安装/卸载向导 UI |
| 构建链 | Node 脚本 | `scripts/`（打包链、图标生成、托盘测试）+ `minify.cjs`（HTML 压缩） |
| 平台 | Windows | 开发环境为 Windows / PowerShell |

**唯一真源声明**：
- 版本号唯一真源：`src-tauri/tauri.conf.json` 的 `version` 字段（`sync-version.cjs` 构建时自动同步到其余五处，**禁止手改任何一处的版本号**）。
- 依赖版本唯一真源：`package.json` / `installer/package.json` / 两处 `Cargo.toml`。
- 设计语言唯一真源：`docs/“去线留白”设计语言.md`；页面级硬性要求见 `docs/AI生成页面需求模板.md`。
- AI 协作流程唯一真源：`skills/` 目录（见 `AGENTS.md`）。

## 2. 核心原则（负面清单）

> 行数 / 分支数阈值为审查软约束，未用 lint 强制。

1. **避免过度设计** — 不为"可能会用到"写抽象。
   - HTML 出现多余的包裹层（如仅透传属性的空 `<div>` 容器）
   - 为单一页面抽取"可复用函数"但实际只调用一次
   - JS 中引入不必要的状态机、事件总线等模式，用普通变量即可表达
   - CSS 中为同一属性定义语义重复的变量（如 `--bg` 与 `--bg-card` 值相同）
   - 引入前端框架或构建工具链，而原生 HTML/CSS/JS 即可表达（**项目硬性要求原生、零依赖**）
   - **不算过度设计**：Tauri window controls 结构、`:root`/`[data-theme="dark"]` 令牌体系（「去线留白」设计语言）、骨架自带的标题栏/滚动/屏蔽右键与快捷键脚本、示例页的 sidebar + `data-page` 切换与 SVG 雪碧图 `<symbol>`/`<use>` 机制、`data-i18n` 标注机制——这些是项目既有的合理架构。

2. **避免冗余代码** — 未使用的代码立即删除，不留"备用"。
   - `:root` 中定义但未被任何选择器引用的 CSS 变量
   - `<style>` 中定义了但 HTML 中未使用的 CSS 类
   - `<script>` 中声明但未调用的函数/变量/常量
   - HTML 中 `id` 定义了但 JS 中从未 `getElementById` 引用
   - 注释掉的整块代码（应直接删除，而非保留注释）
   - 对同一 DOM 元素重复添加不影响效果的类名
   - Rust 中未使用的 `use`、变量或私有函数（`cargo check` 会报，但需检查 `cfg` 分支下隐藏的）

3. **避免无效修改** — 改动不产生实际效果，或超出任务范围。
   - 修改了 CSS 属性但不影响渲染结果（如给已有 `overflow: hidden` 的元素再加 `clip-path`）
   - 在父子两层元素上重复设置相同的尺寸约束
   - 改了非本次任务范围的文件（除非是顺带修复的明确 bug）
   - 格式化与功能变更混杂，应拆分为独立提交
   - Rust command 参数名与前端 `invoke` 调用不一致（Tauri 自动 camelCase↔snake_case，但语义需一致），或新增字段但从未读取

4. **避免复杂逻辑** — 保持可读性，控制规模。
   - HTML 嵌套超过 4 层（骨架 `.content > .content-inner > .card > .form-group > input` 已达极限）
   - `<script>` 中单个函数超过 80 行（示例页演示逻辑允许稍长，但超过 80 行应考虑拆分）
   - 重复的同类条件分支超过 5 个，未用 `switch` 或对象映射表替代
   - 正则表达式（如日期/数字校验）无注释说明匹配意图
   - 硬编码的魔法数字（如 `1024`、`40`、`1000`）未定义为有意义的常量或说明来源

5. **避免过度防御** — 不对"理论上不可能发生"的场景防御。
   - `document.getElementById` 返回值做了 null 检查（该元素在页面中始终存在）
   - 对 `window.__TAURI_INTERNALS__` 判空后，在打包版分支内又重复判空
   - 多层 `try/catch` 嵌套可合并
   - 对 Tauri command 返回的错误做与 Rust 侧 `Result` 重复的兜底
   - **不算过度防御**（正当边界防御）：用户输入校验（格式、范围、留空检测）、浏览器预览降级（`__TAURI_INTERNALS__` 不存在时隐藏标题栏、保留调试键）、Tauri API 调用失败时的错误处理（如 `updateMaxBtn` 的 `catch { /* 忽略 */ }`）、localStorage 读取时的 `try/catch` 与容错。

6. **命名清晰** — 名称表达意图，不靠长度堆砌；风格与项目既有命名保持一致。
   - 函数/变量名超过 4 个词，语义堆叠，应拆分或换用领域词
   - CSS 类名不用 kebab-case，或与既有体系不一致（项目统一 `content-inner`、`win-btn`、`card`、`form-group` 风格）
   - 布尔变量不以 `is` / `has` / `should` / `can` 开头
   - 名称与行为不符（`getXxx` 却含写入副作用；`save` 实际未落盘）
   - 同一概念在同一文件内混用不同名称（`save` / `store` / `persist` 指同一操作）
   - Rust 函数/变量不用 snake_case、常量不用 SCREAMING_SNAKE_CASE
   - `data-i18n` 键名随意（应遵循既有 `区段.条目` 层级风格）
   - **不算违规**（正当命名）：项目既有惯例（`updateMaxBtn`、`apply_window_chrome`、`SHADOW_MARGIN`、`--duration-*` 令牌、`win-btn-min`）；约定俗成短名（`e`、`i18n`、`ctx`）；命名问题的修复在审查阶段一次性列全，实施时同步更新引用，不留"待改"中间态。

## 3. 目录结构与"新代码放哪"

```
dist/index.html        ← 前端唯一权威源（既是源码也是产物，直接修改它）
dist/tray-menu.html    ← 托盘右键菜单页（仅 data-tauri-tray 标记存在时被打包）
components/            ← 参考组件库 demo（不进构建产物；tokens.css 与 dist 令牌共享）
src-tauri/src/lib.rs   ← Rust 壳逻辑（新命令在此定义并注册）
installer/             ← 安装/卸载向导子包（UI 改 installer/src/，Rust 改 installer/src-tauri/src/）
docs/                  ← 文档（设计语言、使用说明、AI 需求模板）
scripts/               ← 构建链脚本
过程性文档/             ← 跨会话方案文档（规范见 plan-doc skill）
skills/                ← AI 协作 skill 唯一维护源
```

**裁决规则**：
- 前端改动一律改 `dist/index.html`；`components/` 是拆分参考，不进构建。
- 设计令牌三处同源（`dist/index.html` 的 `:root`/`[data-theme="dark"]`、`components/tokens.css`、`installer/src/styles/tokens.css`），但为**同源不同子集**：**共享令牌**（三处都有的，如 `--accent`、`--text-*`、`--duration-*`、`--input-bg`）改动须三处同步，禁止单侧改动；各处独有令牌不受此约束（installer 独有 `--tag-*`/`--cursor`/`--ease-standard`/`.dark` 类与 `--font-sans`，components 独有 `--radius`/`--font`/`--titlebar-h`/`--shadow-card`）。installer 侧深色走 `.dark` 类、缓动叫 `--ease-standard`，是既定差异，不是违规。
- Rust 新命令：`lib.rs` 定义 + `generate_handler!` 注册，缺一不可（现注册 `get_file_size`、`apply_window_chrome`、`exit_app`、`show_main_window`）。
- 新增静态资源（`dist/` 下新文件）须在 `minify.cjs` 的打包清单登记（HTML 加进 `PAGES` 列表，非 HTML 加 `copyFileSync`），否则不进安装包。
- 跨会话方案文档放 `过程性文档/`，写作与接力规则见 `plan-doc` skill，此处不重复。

## 4. 前端规范（单文件 HTML / CSS / JS）

完整细则以 `docs/AI生成页面需求模板.md`（硬性要求）与 `docs/"去线留白"设计语言.md`（设计语言）为准，此处为审查对照表：

| # | 检查项 | 违规信号 |
|---|--------|---------|
| 1 | 令牌取色 | 新增颜色值未定义为 `:root`/`[data-theme="dark"]` 令牌，直接在规则中硬编码（如 `#1A8AFF`）；新增多处重用时必须提为令牌 |
| 2 | 去线留白 | 用边框线表达分隔/层级（仅高密度表格行数 ≥10 且列数 ≥3、警示场景可保留）；模块间距非 8px 标尺整数倍（控件内边距等微观尺寸除外）；动效时长/缓动未用 `--duration-*`/`--ease` 令牌，或缺少 `prefers-reduced-motion` 降级 |
| 3 | 响应式 | 引入 `@media` 断点（**禁止**，含窄窗口断点）；窄窗口（最小 600×400）下内容不可用——应靠 flex-wrap、min-width 等弹性布局保障 |
| 4 | 单文件资源 | 外链任何资源（外链 CSS/CDN/外部图片/网络字体）；用 emoji 代替内联 SVG 图标；引入前端框架 |
| 5 | 无障碍 | 交互元素缺 `aria-label`/可访问文本（仅有 `title` 不够）；装饰性 SVG 缺 `aria-hidden="true"`；前景/背景对比度低于 WCAG AA |
| 6 | 窗口交互 | 标题栏新增自定义交互元素点击被拖拽吞掉（非 BUTTON 等可点击标签的元素需 `role`/`tabindex`；Tauri v2 下 `-webkit-app-region` 不生效，勿用）；窗口控制命令串未原样使用骨架的 `plugin:window|*` |
| 7 | 托盘契约 | 托盘相关改动（`data-tauri-tray` 标记、`build.rs`、`lib.rs` 的 tray cfg）三处不一致，或未跑 `cargo test` / `npm run test:tray` 验证 |
| 8 | 范围克制 | 功能超出用户需求（多页面、设置项、统计、导入导出、localStorage 等未要求即加）；使用原生 `alert`/`confirm`/`prompt`；删除/清空等危险操作无页面内确认 |

## 5. Rust 后端规范

- 命令命名 snake_case，前端 `invoke` 侧 camelCase 自动映射，语义必须一致。
- 新命令必须同时出现在 `lib.rs` 定义与 `generate_handler!` 注册列表（审查时核对）。
- 壳层保持轻逻辑：能用前端表达的不下沉到 Rust；不下沉构建期可完成的逻辑（如托盘开关由 `build.rs` 条件编译而非运行时判断）。
- 托盘契约三处一致（`dist/index.html` 标记 / `build.rs` / `lib.rs` cfg），由 `cargo test` 的 `tray_cfg_matches_html_marker` 锚定。
- 错误处理从简：壳层命令直接返回值或 `Result`，前端不做与 Rust 侧重复的兜底。

## 6. 安装器子包规范（installer/）

- 独立 Tauri 2 + React 19 + TypeScript + Tailwind CSS 4 子包，依赖在首次打包时由构建脚本自动安装。
- UI 组件复用 `installer/src/components/ui/`（Button/Modal/Switch）；文案配置集中在 `installer/src/product.ts`，产品常量集中在 `installer/src-tauri/src/config.rs`——改名/定制只动这两处。
- 样式令牌与主项目共享（`installer/src/styles/tokens.css`），与 `dist/index.html`、`components/tokens.css` 三处同步（见第 3 节）。
- Rust 命令分两层：`#[tauri::command]` 薄封装定义在 `lib.rs` 内嵌的 `mod ipc`（注册也在此处 `generate_handler!`，注册名带 `ipc::` 前缀），实际逻辑在 `commands/` 按域分文件（install/uninstall/registry/shortcut 等）。新命令 = `commands/` 写实现 + `ipc` 模块加封装 + `generate_handler!` 注册，三处缺一不可。

## 7. 测试

- **Rust**：`src-tauri/` 下 `cargo test`（托盘标记一致性测试）。
- **托盘契约**：`npm run test:tray`（`scripts/test-tray-opt-in.mjs`，双用例验证；注意该脚本会临时改写 `dist/index.html` 与 `dist-min/` 后自动还原，非纯只读）。
- 无前端单测框架：前端逻辑改动靠 code-review + 手动验证（`npm run tauri dev` 预览）兜底；安装器前端有 `npm --prefix installer run typecheck` 可作类型验证锚点。
- **提交前门禁**：涉及 `src-tauri/` 或托盘相关改动 → `cargo check` + `cargo test` 必须通过；涉及托盘契约 → 加跑 `npm run test:tray`；涉及 `installer/src-tauri/` → 在该目录下 `cargo check`；涉及 `installer/src/` → `npm --prefix installer run typecheck` 通过。

## 8. Git 提交规范

提交信息格式与生成流程见 `commit-message-generator` skill（`skills/commit-message-generator/`），分支命名见 `create-branch` skill，此处不重复词表。

## 9. 开发流程

**提交前检查清单**：
1. 改动范围内 `cargo check`（src-tauri/ 或 installer/src-tauri/ 下）通过
2. 涉及 Rust 或托盘 → `cargo test` 通过；涉及托盘契约 → `npm run test:tray` 通过；涉及 installer 前端 → `npm --prefix installer run typecheck` 通过
3. 无 `核心原则` 各条违规信号（见第 2 节）
4. 共享设计令牌改动已三处同步（见第 3 节令牌规则）

**版本号机制**：只改 `src-tauri/tauri.conf.json` 的 `version`，构建时 `sync-version.cjs` 自动同步到 `package.json`、`Cargo.toml` 及安装器子包三处，共六处。

**构建命令**：
- `npm run build`：完整安装器打包链（主应用构建 → 安装向导编译 → 主程序嵌入），产物 `dist-installer/*_setup.exe`
- `npm run build:nsis`：NSIS 备用方案；`npm run tauri build -- --no-bundle`：裸 EXE
- 构建时自动执行：版本号同步 → 图标生成（`logo.svg` 单源）→ HTML 压缩（`minify.cjs` 白名单）

**PowerShell / Git 坑位**（AI 协作时）：
- git diff 中文内容禁止用 `>` 重定向（PowerShell 会写成 UTF-16），用 `git diff --output=<file>` 落盘
- 中文路径加 `-c core.quotepath=false`；diff 加 `--no-color`
- 终端中文乱码时前缀 `[Console]::OutputEncoding = [Text.Encoding]::UTF8;`

## 10. AI 协作规范

- **AI 生成的代码等同于新人 PR**：必须经过 code-review 流程（`skills/code-review/`）审查后才可合入，标准不因"AI 写得快"而放宽。
- **审查三问**（三类改动合入前必须能讲清实际行为，讲不清楚即打回）：
  1. **异步时序**：Tauri command 调用、窗口创建/显示时序、构建脚本链的先后依赖是否正确？
  2. **边界与异常**：浏览器预览降级、托盘标记缺失、文件不存在、用户非法输入等边界是否处理且不过度？
  3. **核心算法**：改动的核心逻辑（解析、计算、状态判定）实际行为是什么，与预期一致吗？
- **实证原则**：涉及 Tauri 内部行为或第三方 crate 的结论必须读源码/文档验证，验证结论必须用命令退出码锚定（见 code-review skill）。
- **人机分工**：用户负责需求决策与结果审查，AI 负责执行与自查；方案级决策（选型、行为边界）必须用户拍板，代码级细节 AI 自主。
- 给 AI 布置页面生成任务时，随任务提供 `docs/AI生成页面需求模板.md`。
